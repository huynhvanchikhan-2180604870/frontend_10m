
const initialState = {
  user: null,
  loading: false,
  error: null,
  jwt: null,
};
export const autReducer = (state = initialState, action) => {
    switch (action.type) {
      default:
        return state;
    }
}